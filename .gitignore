# dependencies / build output
node_modules/
dist/
dist-app/
*.tsbuildinfo

# media (too large for git) - see docs/OPERARE.md and production/
media/*.mp4
media/*.mkv
media/*.mov
media/*.log
media/analysis/*.png
!media/analysis/.gitkeep

# runtime
runs/
logs/
*.log
.env
.env.*
!.env.example
config.json
config.wall.local.json
config.auto.local.json
config.wall.local.json.*.tmp
!config.example.json

# voice cache produced at runtime (pre-generated voices under assets/voice ARE committed)
cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# continuity heartbeat (scripts/heartbeat.ps1) + unit test bundles
HEARTBEAT.log
.tmp-tests/
# Disposable QA bundles and language-tool caches
/.tmp-*/
/coverage/
__pycache__/
*.py[cod]
data/users.json
data/sessions.json
data/nava.sqlite*
data/diagnostics/
data/installations/
.venv-optical/

# Audio production intermediates: WAV masters, QA renders and raw API responses stay local (runtime uses the MP3s in assets/music/)
assets/music/masters/
assets/music/qa/
*.api-pcm
data/audit*.jsonl

# Local process notes, retired code, prompts and distribution archives.
# Cleanup inventory and SHA-256 manifest live under AI/archive/.
/AI/
/Old/

# Test/distribution archives built from dist-app
*.zip
