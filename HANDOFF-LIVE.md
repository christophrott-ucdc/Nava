# HANDOFF-LIVE — jurnal de lucru în timp real (append-only)

> **Pentru Codex sau orice AI care preia lucrul dacă sesiunea Claude se oprește la mijloc.**
> Acest fișier spune ce se lucrează ACUM, de către cine, ce s-a terminat și ce a rămas.
> Se completează prin **append** (niciodată rescris). Mecanic, `scripts/heartbeat.ps1` scrie la fiecare
> 60 s în `HEARTBEAT.log` (rădăcină, negit-uit): fișierele modificate în ultimul minut, numărul de
> fișiere necomise și ultima linie de aici. Dacă `HEARTBEAT.log` nu mai primește linii, sesiunea a murit.
>
> Cum continui: 1) citește `HANDOFF.md` §0 și acest fișier de la coadă la cap; 2) rulează `npm run check`;
> 3) uită-te în `git status` ce e necomis; 4) ia pachetele marcate `[ ]` din §2 de mai jos, în ordinea lor;
> 5) după fiecare pas, adaugă o linie în §3.

## 1. Contextul rundei (2026-09-04, seara)

Cererea lui Christoph: „Repară tot și implementează tot (schelet unde nu se poate). PIN 4078, utilizatori,
pagină de depanare. Agenți în paralel. Heartbeat la 1 minut. Claude face partea grea."

Punctul de plecare: commit `5af8383` pe `board/nava-player` — build/typecheck curate, 51 voci V3
ElevenLabs în `assets/voice/ro`, exe portabil în `dist-app/`. Auditul Claude a găsit 15 probleme și a
propus 15 funcționalități (vezi §2). Contractele noi sunt în `src/shared/types.ts` și `src/shared/protocol.ts`
(secțiunile marcate `R4` = runda 4).

## 2. Pachete de lucru și proprietari

Legendă: `[ ]` neînceput · `[~]` în lucru · `[x]` gata · `[s]` doar schelet. Fișierele din paranteză sunt
singurele pe care pachetul are voie să le modifice. `src/server/index.ts` se integrează DOAR de orchestrator.

### Orchestrator (Claude) — securitate, utilizatori, depanare, integrare
- [x] P-01 Autentificare: PIN operator (implicit **4078**), sesiuni, cookie, middleware pe `/api/cmd`, `/api/show/*` scriere, `/api/users`, `/api/tablets/clear`, `/api/debug/*`, `/api/tts`; WS `hello` cu `token` pentru `control`, `screenToken` pentru `screen` (`src/server/auth.ts`, `src/server/users.ts`)
- [x] P-02 Utilizatori: `data/users.json` (scrypt), roluri `admin|operator|viewer`, CRUD, admin implicit cu PIN 4078 (`src/server/users.ts`, `src/web/login/**`)
- [x] P-03 Pagina de depanare `/debug`: stare, clienți, cue-uri, preflight voci, perf ecrane, drift, loguri, config redactat, acțiuni (`src/server/debug.ts`, `src/web/debug/**`)
- [x] P-04 Preflight active: verifică cele 51 de clipuri (există + dimensiune + durată) la pornire și la cerere; semnal roșu în consolă/debug (`src/server/preflight.ts`)
- [x] P-05 Rotația jurnalelor `runs/` (păstrează ultimele 20 rulări, mută PNG în `runs/debug/`) (`src/server/runlog.ts` — coordonat cu D)
- [~] P-06 Integrare `src/server/index.ts`, `npm run check`, commit pe `board/nava-player`

### Agent A — main/Electron: producție kiosk, span-mode, autostart
- [ ] A-01 `powerSaveBlocker`, watchdog (repornire renderer + relansare app la eroare fatală), `config.autostart` → `setLoginItemSettings`, `scripts/install-autostart.ps1` (Task Scheduler) (`src/main/**`, `scripts/install-autostart.ps1`)
- [x] A-02 `displayMode: "span"` — o singură fereastră peste toate ecranele; renderer-ul primește lista de viewport-uri per ecran în boot (`src/main/windows.ts`, `src/main/ipc.ts`, `src/preload`)
- [x] A-03 Boot: `screenToken`, `serverHttpUrl`, `security`, `displayMode`, `viewports` în `getBoot()` (`src/main/ipc.ts`, `src/preload/preload.ts`)
- [x] A-04 Config loader: câmpuri noi cu valori implicite (`security`, `ambient`, `lights`, `autoRun`, `displayMode`, `screens[].yawOffsetDeg`, `variant`) (`src/main/config.ts`, `config.example.json`, `config.5screens.example.json`, `config.follower.example.json`)
- [ ] A-05 Rotația jurnalelor `app-*.jsonl` în main (`src/main/logger.ts`)

### Agent B — renderer: perf, audio routing, ambianță, entități reactive, perspectivă laterală, rehearse, span
- [x] B-01 `setSinkId(config.audio.outputDeviceId)` + listă dispozitive raportată în `perf`/debug (`src/renderer/voice/context.ts`, `src/renderer/index.ts`)
- [x] B-02 Mesaj `perf` la 1 Hz de la fiecare ecran: cadre pierdute, fps video, fps avatar, latență lip-sync, drift, heap (`src/renderer/perf.ts`, `src/renderer/sync.ts`)
- [x] B-03 Ambianță procedurală per temă cu ducking sub voce; cue `ambient`; comandă `ambient` (`src/renderer/voice/ambient.ts`, `timeline.ts`)
- [x] B-04 Entități reactive la alegerile tabletelor: mesaj `entityParams` (culoare / puls / perspectivă) (`src/renderer/ui/entities.ts`, `sync.ts`)
- [x] B-05 Perspectivă laterală: `screens[].yawOffsetDeg` → crop/shift orizontal al filmului pe ecranele laterale (`src/renderer/player.ts`)
- [x] B-06 `rehearse`/`setRate`: redare accelerată (video rate + voci comprimate `playbackRate`) (`player.ts`, `voice/playback.ts`)
- [s] B-07 Span-mode: un `<video>` decodat o singură dată, desenat pe N canvas-uri per viewport (`src/renderer/span.ts`)
- [x] B-08 Cue `dynamic-voice` / mesaj `dynamicVoice`: redare text live prin `/api/tts` (`timeline.ts`)
- [s] B-09 Schelet: cue `photo` (webcam → dataURL → server), detector zgomot sală (`roomLevel` în `perf`) (`src/renderer/photo.ts`, `src/renderer/room-mic.ts`)
- [x] B-10 Teste `src/renderer/**/*.test.ts` pentru timeline (seek înainte/înapoi, lead-in negativ) — rulate cu `npm test`

### Agent C — avatar & voce: casting, viseme precalculate, limbi, dialog live (schelet), variante
- [ ] C-01 Casting: `config.avatar.body`, `avatar.glbBySpeaker`, mesaj clar în debug când vocea și corpul nu se potrivesc; ghid de obținere GLB Căpitan (Avaturn/RPM) în `docs/AVATAR.md` (`src/renderer/avatar/**`)
- [ ] C-02 `scripts/precompute-visemes.mjs`: `words` → `visemes/vtimes/vdurations` cu `lipsync-ro` în manifest; renderer preferă viseme (`src/renderer/avatar/**`, `assets/voice/ro/manifest.json`)
- [ ] C-03 Limbi: `setLang` acceptă doar limbi cu manifest complet; EN/FR marcate indisponibile în consolă; `scripts/tts-generate.mjs --lang en|fr` documentat (`src/renderer/voice/manifest.ts`)
- [ ] C-04 Măsurarea latenței lip-sync (audio start vs primul visem) raportată în `perf` (`src/renderer/avatar/index.ts`)
- [ ] C-05 Schelet dialog live: Web Speech API (STT ro-RO) → `POST /api/dialog` (Gemini, personaj Căpitan) → `say` (`src/renderer/voice/live-dialog.ts`, `src/server/features/dialog.ts`)
- [ ] C-06 Variante pe vârstă: `VoiceCue.variants`, `ShowFile.variants`, `--variant` în `tts-generate.mjs`; schelet cu o variantă „7-9" pentru 3 replici (`scripts/tts-generate.mjs`, `assets/show/show.json` doar câmpuri noi)
- [ ] C-07 Teste `lipsync-ro` (`src/renderer/avatar/lipsync-ro.test.ts`)

### Agent D — server (fără index.ts) + consolă + tablete: readiness, editor cue, analitică, certificate, telemetrie, lumini, auto-run
- [x] D-01 Readiness gate în `state.ts`: `preshowAutoStart`/`autoRun` pornesc doar când ecranele cerute sunt conectate, video gata, preflight ok; `ShowState.readiness` (`src/server/state.ts`)
- [x] D-02 Comenzi noi: `rehearse`, `setRate`, `autoRun`, `lights`, `ambient`, `say`, `setVariant`, `photo`; difuzare `entityParams` din alegerile tabletelor (`state.ts`, `cues.ts`, `tablets.ts`)
- [x] D-03 Mesajele copiilor citite de Căpitan: colectare → `dynamicVoice` la cue `dynamic-voice` (`src/server/features/dynamic-voice.ts`)
- [x] D-04 Editor de cue-uri în consolă: drag pe timeline, `POST /api/show` (salvare + backup + reload) (`src/server/features/show-editor.ts`, `src/web/control/**`)
- [x] D-05 Analitică: `/analytics` din `runs/show-*.jsonl` (`src/server/features/analytics.ts`, `src/web/analytics/**`)
- [x] D-06 Certificat de misiune pe tabletă (canvas → PNG, descărcare + QR) (`src/web/tablet/**`)
- [x] D-07 Telemetrie fictivă pe tablete sincronă cu timpul (`src/web/tablet/**`)
- [x] D-08 Schelet lumini: adaptor `artnet` (UDP) și `hue` (REST) pe cue `lights`/`theme` (`src/server/features/lights.ts`)
- [x] D-09 Mod operator absent (`autoRun`) + buton fizic/tabletă de start (`state.ts`, `src/web/tablet/**`)
- [x] D-10 Consola: login cu PIN (cookie), `token` în `hello`, butoane noi (rehearse, ambient, lumini, say, variant), panou readiness/preflight (`src/web/control/**`)
- [x] D-11 Teste `state.ts` (`src/server/state.test.ts`)

### Agent E — documentație consolidată
- [x] E-01 `HANDOFF.md` rescris într-o singură versiune curentă (V3 + runda 4); addendele mutate în `docs/history/HANDOFF-ISTORIC.md`
- [x] E-02 `docs/BRIEF.md`, `docs/OPERARE.md`, `docs/SPEC-SHEET.md`, `docs/DECIZII.md`, `README.md` actualizate (PIN, utilizatori, debug, span, follower, autostart, heartbeat)
- [x] E-03 `docs/SECURITATE.md` (model de amenințare LAN, PIN, token ecrane, ce NU este protejat)

## 3. Jurnal (append-only; format: `- [HH:MM] AGENT · pachet · ce s-a făcut · ce urmează`)

- [22:27] CLAUDE · setup · creat HANDOFF-LIVE.md, heartbeat.ps1, contracte R4 în types/protocol, runner de teste · urmează lansarea agenților A–E și P-01..P-06
- [22:38] A · A-04 · config.ts: deep-merge + validare R4 (displayMode/autostart/security/ambient/lights/autoRun/variant, screens[].yawOffsetDeg, avatar.body/glbBySpeaker), screenToken generat (32 hex) si scris inapoi in config.json pe master, placeholder respins pe follower, chei $comment ignorate, flag --kiosk; config.example.json + config.5screens.example.json + config.follower.example.json · urmeaza A-02/A-03 (span + boot R4)
- [22:42] CLAUDE · P-01..P-05 · auth.ts (PIN login, sesiuni persistente data/sessions.json, cookie nava_session, Bearer, roluri viewer<operator<admin, screenToken), users.ts (scrypt, admin implicit 4078, CRUD /api/users), preflight.ts (51 voci + film + GLB), maintenance.ts (rotație runs, păstrează 20), debug.ts (PerfStore, /api/debug/*, /api/frame cu ffmpeg), web/login + web/debug; integrate în src/server/index.ts (guards pe /api/cmd, /api/show/*, /api/tablets/clear, /api/tts, /api/dialog; WS hello autentificat; perf/photoCaptured; preflight la pornire) · urmează build.mjs (login/debug/analytics), smoke-auth, integrarea rapoartelor A–E
- [22:44] E · E-01 · HANDOFF.md rescris (17 secțiuni, V3 + R4, ~602 linii); addendele §16–§28 mutate verbatim în docs/history/HANDOFF-ISTORIC.md (diff verificat) · urmează E-02: OPERARE, SPEC-SHEET, DECIZII, README, CUE-SHEET
- [22:44] CLAUDE · P-06 (parțial) · scripts/smoke-auth.mjs (30 verificări: guards 401/403, login 4078, CRUD utilizatori, PIN unic, sesiuni invalidate la schimbare PIN, WS hello 4401, rate limit 429) = ALL OK; build.mjs bundlează web/login, web/debug, web/analytics; npm run check include smoke:auth · urmează: integrarea rapoartelor A–E (INTEGRATION.md de la D), npm run check complet, test Electron, commit
- [22:46] A · A-03 · getBoot() intoarce si serverHttpUrl (master http://127.0.0.1:<port>, null daca serverul a cazut; follower derivat din masterUrl ws->http fara /ws), screenToken, security{publicState}, displayMode, viewports (doar span), variant (null = baza); preload pass-through neschimbat · urmeaza A-01/A-05
- [22:46] A · A-02 · windows.ts: displayMode span = O fereastra frameless (fullscreenable false, alwaysOnTop+skipTaskbar in kiosk, F11 dezactivat) peste uniunea DIP a display-urilor din screens[] (screen.getAllDisplays sortate x,y); viewports = bounds display - origine uniune, scaleFactor per display; in --windowed uniunea e scalata pe primar; boot.screen = ecranul cu playAudio (center); --screen <id> ramane in windows mode; avertizari la DPI diferite / display-uri partajate. Verificat: boot windows + span pe 1 display (viewports logate) · urmeaza A-01/A-05
- [10:17] CLAUDE · P-06 · reluare după rate-limit: reparat dynamic-voice.ts (regex rupt), teste lipsync (două mii, sil la cuvinte sărite), show-editor tipuri Hono, etichete comenzi consolă; index.ts montează show-editor/certificates/dialog/lights + hooks director (dynamicVoice/photo/lights/preflight, entityParams, requestStart); sync.ts trimite screenToken; consola: /api/auth/me → token în hello, redirect la /login la 401/4401. tsc curat, 54 teste OK, build OK, smoke-auth/core OK. Electron verificat live: screen:center autentificat, preflight 51/51 (51 viseme), readiness ready, login 4078, /api/debug OK. Commit checkpoint cbd2929 pe board/nava-player. Agenți relansați: B (renderer B-01..B-09), D (D-04..D-11 UI+analytics+teste), E (E-02/E-03/AVATAR.md) · urmează: integrarea analytics la /api/analytics, npm run check, dist exe, commit final
- [10:22] E · E-02 · docs/OPERARE.md rescris (12 secțiuni: login PIN 4078 + schimbare, roluri, /debug, preflight/readiness, windows vs span, follower cu screenToken, autostart Task Scheduler/config.autostart, rehearse, ambianță/lumini/say/variante/foto/analitică cu starea reală a pachetelor, heartbeat) · urmează SPEC-SHEET, DECIZII, README, CUE-SHEET
- [10:26] E · E-02 · docs/SPEC-SHEET.md rescris: FR-11..FR-24 + NFR R4, referință completă config (toate secțiunile R4 cu implicite/limite), schema celor 4 cue-uri noi, tabelul rutelor HTTP cu rolul minim, mesajele WS R4; notă: loadShowFile/validate-show.mjs încă resping cele 4 tipuri noi de cue (fix la orchestrator) · urmează DECIZII, README, CUE-SHEET
- [10:25] D · D-10 · consola: butoane R4 (REPETIȚIE ×4 / VITEZĂ NORMALĂ, AMBIANȚĂ, AUTO-RUN, LUMINI, VARIANTĂ, SPUNE, FOTO, PREFLIGHT), panou readiness din state.readiness (START galben + confirmare când nava nu e gata), panou perf din perfSummary, header cu variantă/autoRun/ambianță/lumini, linkuri /debug/ /analytics/, IEȘI (logout), panou utilizatori (doar admin, /api/users) (src/web/control/index.html, index.ts, styles.css) · urmează D-05 analitică
- [10:25] D · D-04 · TIMELINE EDITOR în consolă (src/web/control/editor.ts): 3 piste (preshow / film cu lead-in negativ / epilog), markere draggable cu snap 0,1 s + săgeți, câmp „la”, SALVEAZĂ = PUT /api/show cu întreg ShowFile (erori/avertismente server inline), ANULEAZĂ = GET /api/show, backup-uri GET /api/show/backups + POST /api/show/restore/:file, previzualizare cadru /api/frame?t=&w=480 (ascuns la 404); tsc curat pe fișierele D (eroarea din src/renderer/player.ts e a lui B) · urmează D-05
- [10:31] E · E-02 · docs/DECIZII.md: ADR-10 actualizat + ADR-15..21 noi (PIN+utilizatori, readiness server-autoritar, span o decodare, viseme precalculate, ambianță procedurală, schelet-first lumini/foto/dialog, HANDOFF-LIVE+heartbeat); README.md rescris scurt (adrese + PIN 4078, config exemple, voci + viseme, dist + autostart, structură R4); docs/CUE-SHEET.md regenerat cu node scripts/build-cue-sheet.mjs — identic (87 cue-uri, show neschimbat). E-02 gata · urmează E-03 SECURITATE.md, apoi AVATAR.md
- [10:35] B · audit · B anterior lăsase module NEcablate: voice/ambient.ts (motor complet, dar createAmbient nu era apelat; Timeline avea dep `ambient?` nefolosit), perspective.ts (matematică + teste, neaplicată pe <video>), voice/context.ts (routeAudioOutput/setSinkId scris, neapelat), voice/index.ts (setTtsAuthToken exportat, neapelat → /api/tts fără Bearer), cue-scheduler.ts + teste OK, playback.ts avea deja calea preservesPitch pentru rehearse · urmează B-02..B-09
- [10:35] B · B-02 · src/renderer/perf.ts (PerfMonitor 1 Hz: dropped/total, fps video prin requestVideoFrameCallback, avatarFps/lipsyncLatencyMs din avatar, drift, roomLevel, heap, audioOutput) + SyncClient.sendPerf/sendRaw; linie „perf" în OSD (index.html/osd.ts) · urmează B-08
- [10:35] B · B-08 · sync.ts rutează `dynamicVoice` → Player.speakDynamic → Timeline.speakDynamic (getClip prin manifest/ /api/tts cu Bearer screenToken — setTtsAuthToken(boot.screenToken) în index.ts, FX vorbitor, lip-sync Căpitan, subtitrare dacă subtitle≠false, preemptează vocea curentă); `applyCmd say` direct → același traseu (id say-<fnv>) · urmează B-06
- [10:35] B · B-06 · Player.setRate (clamp 0.25..8): video.playbackRate, PhaseClock cu rată (lead-in/preshow/epilog/countdown scalează), setVoicePlaybackRate (element HTMLAudio preservesPitch la rată≠1, cap 2.5), nudge follower în jurul ratei nominale, `report.rate` = rata nominală; badge „REPETIȚIE ×N" (#rehearse, pe toate ecranele) + în OSD; welcome adoptă state.rate/variant/ambientEnabled; `setVariant` → Timeline (text variants[v][lang], clip `<id>.<v>`) · urmează B-04
- [10:35] B · B-04 · entities.setParams/getParams: LUMINA tint (paletă din culoare), NATURA pulseBpm (bătaie peste amplitudine) , TEHNOLOGIC perspective (rotație+skew determinist pe cheie), intensity/votes → densitate 0.5..1.4 (particule/picături/segmente/inele); persistă până la `hide`/`hideAll`; sync `entityParams` → Player.setEntityParams; teste ui/entities.test.ts · urmează B-01
- [10:35] B · B-01 · index.ts apelează routeAudioOutput(config.audio.outputDeviceId) după crearea motorului vocal (deviceId exact SAU substring de etichetă; „default" = implicit; niciodată nu aruncă); eticheta → PerfSample.audioOutput · urmează B-05
- [10:35] B · B-05 · Player.refreshLayout: yawTransform(screen.yawOffsetDeg) → transform translateX+scale pe <video> (clamp ±40 %), la attach/loadedmetadata/resize; dezactivat în span (canvasele fac yaw). Ecranele fără showAvatar ascund deja #avatar (nemodificat) · urmează B-03
- [10:35] B · B-03 · createAmbient în index.ts (audible=playAudio, config.ambient cu implicite R4), theme.onChange → followTheme, ducking din Timeline.speak (exista), cue `ambient` (exista), comanda `ambient` → setEnabled, setVolume sfx → ambient.setSfxVolume; NOTĂ: patul temei implicite cântă și în idle (tema prolog) · urmează B-07
- [10:35] B · B-07 · schelet src/renderer/span.ts: displayMode=span + boot.viewports → <video> opacity 0 (decodează), un <canvas> per viewport desenat cu requestVideoFrameCallback (fallback rAF) folosind yawSourceRect (fit+yaw per ecran), overlay-urile mutate în #span-focus (viewport-ul ecranului cu showAvatar/showSubtitles); limite documentate în antet (unități vw/vh, OSD la nivel de fereastră, DPR unic); testat 1 viewport doar prin unit-teste (pickFocusViewport/canvasBacking) · urmează B-09
- [10:35] B · B-09 · schelet photo.ts (mesaje `photo`: countdown → capture doar pe ecranul sursă de ceas/center via getUserMedia → JPEG ≤1280 px → `photoCaptured`; show/hide; fără cameră doar avertizează) + room-mic.ts (doar cu ?mic=1; RMS → perf.roomLevel) · urmează B-10
- [10:35] B · B-10 · teste: perf.test.ts (computeFps/formatPerfLine/fitWithin/rmsToLevel/span helpers), ui/entities.test.ts; cue-scheduler + perspective existente; npm test 70/70, tsc curat, build OK. NECOMIS (integratorul comite pe board/nava-player). Pentru integrator: serverul trebuie să difuzeze applyCmd pentru rehearse/setRate/ambient/setVariant (Player le aplică pe toate ecranele); `say` → dynamicVoice deja; nicio modificare în src/shared
- [10:36] E · E-03 · docs/SECURITATE.md nou (8 secțiuni: model de amenințare LAN cu actori, ce protejează PIN-ul pe rol, ce rămâne public incl. show-ul întreg în welcome către tablete, sesiuni TTL 720/invalidare la PIN, rate limit 8/5 min, screenToken + excepția „gol pe master", recomandări, lacune: HTTP fără TLS, cookie fără Secure, /api/auth/me întoarce tokenul, CORS *, /api/certificates cere operator deci tabletele nu pot posta). E-03 gata; docs/BRIEF.md NU a fost atins (proprietar: orchestrator) · urmează docs/AVATAR.md, apoi HANDOFF.md §14
- [10:30] D · D-05 · src/server/features/analytics.ts (parseRunLines/summarizeRun/aggregateRuns pure + createAnalyticsRouter({runsDir,log}): GET /summary, /runs, /run/:id; verificat pe cele 21 jurnale reale) + src/web/analytics/{index.html,index.ts,styles.css} (carduri, bare pe canvas, tabel rulări, detalii pe rulare, 401 → /login/?next=/analytics/); src/server/features/INTEGRATION.md scris (montare: app.route("/api/analytics", createAnalyticsRouter({ runsDir: opts.runsDir, log })) — gardurile viewer există deja) · urmează D-06 certificat pe tabletă
- [10:33] E · C-01 (doc) · docs/AVATAR.md nou (9 secțiuni: casting GLB feminin vs voce masculină + avertismentul exact, cele 15 viseme Oculus și numele morph target-urilor, Avaturn/RPM/Blender, config avatar.body/glbBySpeaker, verificare prin log/getCastingReport/testAvatar, lipsync-ro + precompute-visemes (51/51, fără alias npm), EN/FR cu manifeste goale + LangGuard, variante 7-9 fără audio și fără --variant). Checkbox-ul C-01 nu e al meu — orchestratorul decide · urmează HANDOFF.md §14
- [10:33] E · E-02/E-03 închidere · HANDOFF.md §14 rescris ca fotografie 10:30 a HANDOFF-LIVE §2 + fișierele observate (A-01/A-05, C-01..C-05/C-07, D-01/D-02/D-03/D-08/D-09 au cod dar nu sunt bifate); §3 check include smoke:auth; §7 casting/viseme actualizate (51/51). OPERARE/SPEC/AVATAR corectate după livrările B (perf, rehearse, say, span/photo schelet). Contradicții semnalate: loadShowFile + validate-show.mjs resping cele 4 cue-uri noi; /api/analytics nemontat; POST /api/certificates cere operator (tabletele nu pot posta); createLangGuard nefolosit de player · urmează: nimic (E gata); docs/BRIEF.md rămâne la orchestrator
- [10:40] D · D-06 · src/web/tablet/certificate.ts: certificat 1200×1700 pe canvas la interacțiunea `thanks` (titlu, post + lentilă, alegerile confirmate ale perechii pe zone A/B strânse din tabletView.zoneChoices pe parcursul misiunii, data, replica de încheiere), SALVEAZĂ (toDataURL → <a download>), POST /api/certificates {post,dataUrl} → „trimis operatorului”; la 401/403 (ruta e în lista operator din index.ts, tabletele n-au sesiune) afișează fallback — vezi INTEGRATION.md §2 pentru gardul recomandat · urmează D-07
- [10:40] D · D-07 · src/web/tablet/telemetry.ts: „consolă de post” cu 4 instrumente per post (NAVIGAȚIE busolă/traseu/distanță/viteză, PROPULSIE energie/stabilitate/impuls/temperatură, COMUNICAȚII semnal/undă/frecvență/cuvinte, BIOSEMNALE ECG/bpm/oxigen/legătură, MEMORIE jurnalul subtitrărilor/memorie/marcaj/amintiri), animate la 10 fps din `state` 1 Hz (phaseTime + rate, offset de ceas estimat) și din temă; vizibilă între interacțiuni; verificată vizual în harness local · urmează D-09
- [10:40] D · D-09 · tabletă: buton mare „PORNEȘTE MISIUNEA” la postul 1 când state.state==="idle" && state.autoRun, trimite {kind:"choice", cueId:"__start__", zone:"A", value:"start"}; arată motivele din state.readiness când nava nu e gata; eroarea serverului reface butonul. Bonus: src/web/tablet/index.html încărca `index.js` (bundle-ul e app.js) — corectat · urmează D-11
- [10:40] D · D-11 · src/server/state.test.ts (node:test, 16 teste, ceas + scheduler injectate prin DirectorOptions): readiness (ecrane/tablete/video/preflight, setCounts legacy), start la −launchLeadInSec + cue-uri la −10/0/9, ceas virtual (pauză/redare/seek, salt >3 s = seek), sfârșit video → epilog → ended, rehearse ×4 / setRate, autoRun immediate/gated/tablet trigger/resetAfterSec, say/lights/setVariant/ambient/photo, validateCommand R4. `npm test` = 86/86; tsc curat pe fișierele D; build OK (analytics bundlat). Toate pachetele D livrate — rămâne montarea /api/analytics de către orchestrator (INTEGRATION.md §1) · gata
- [10:45] CLAUDE · FINAL · toți agenții au livrat (B: perf/dynamicVoice/rehearse/entități/perspectivă/ambianță/span[s]/foto[s]; D: consolă R4 + editor timeline + analitică + certificat + telemetrie + start tabletă + teste state; E: OPERARE/SPEC/DECIZII/SECURITATE/AVATAR/README/HANDOFF §14). Orchestrator: loadShowFile → validateShowFile (cele 4 tipuri noi de cue acceptate la pornire), validate-show.mjs acceptă noile tipuri, certificatele POST anonime (GET = viewer), /api/analytics montat, smoke-platform autentificat cu PIN. npm run check = VERDE (tsc, show, voci, build, 86 teste, smoke core/auth/platform/media). Rămase ca schelet: C-03 (gardă limbi neconectată în player), C-05 (dialog live: STT+/api/dialog există, UI de pornire lipsă), C-06 (variants în show.json + manifest, dar fără --variant în tts-generate), B-07 span (1 viewport verificat), B-09 foto/mic. Casting-ul avatarului rămâne o decizie a lui Christoph (GLB feminin vs Căpitan masculin; vezi docs/AVATAR.md). Pas următor: npm run dist (în curs), verificare vizuală consolă, commit final.
- [10:47] CLAUDE · DIST · npm run dist OK → dist-app/NavaPlayer-0.1.0-x64-portable.exe + NavaPlayer-0.1.0-x64-setup.exe (R4). Verificat live: login → consolă (admin), /debug, /analytics (21 rulări). Runda 4 închisă; vezi §2 pentru pachetele rămase ca schelet [s].
